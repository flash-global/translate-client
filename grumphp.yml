parameters:
    git_dir: .
    bin_dir: vendor/bin
    ascii:
        succeeded: ~
        failed: ~
    tasks:
        phplint: ~
        phpcs:
            encoding: UTF-8
        phpcpd:
            exclude: ['vendor']
        codeception: ~
